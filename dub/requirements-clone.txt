# Extra deps for --clone mode (voice-preserving dub).
# Install CUDA-enabled torch FIRST so both packages use the GPU:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
demucs>=4.0
coqui-tts>=0.24
